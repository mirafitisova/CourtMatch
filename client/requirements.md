## Packages
framer-motion | Smooth animations for page transitions and interactions
date-fns | Formatting dates for match schedules and history
lucide-react | Iconography for the UI (already in base, but ensuring availability)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
